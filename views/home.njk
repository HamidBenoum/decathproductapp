{% extends "layout.njk" %}

{% block content %}
  <div>
    <table class="table table-striped">
  <thead class="text-light bg-dark">
    <tr>
      <th scope="col-8">Categories</th>
    </tr>
  </thead>
  <tbody>
      {% for categorie in categories%}
      <tr>
        <td><a href="http://localhost:3000/categories/{{categorie.id}}/products">{{categorie.label}}</a></td>
      </tr>
      {%endfor%}
  </tbody>
</table>
  </div>
{% endblock %}
